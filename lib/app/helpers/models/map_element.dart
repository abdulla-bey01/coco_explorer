//using to pass key-value pair to map
//example using is existed in ICocoNetworkManager
class MapElement {
  String key;
  dynamic value;
  MapElement({
    required this.key,
    required this.value,
  });
}
